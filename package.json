{
	"name": "mock-keycloak-api",
	"version": "1.0.0",
	"description": "Mock Keycloak OpenID Connect API built on json-server (local + Netlify Functions).",
	"private": true,
	"main": "server.js",
	"scripts": {
		"start": "node server.js",
		"dev": "node --watch server.js"
	},
	"dependencies": {
		"dotenv": "^17.4.2",
		"json-server": "^0.16.3",
		"jsonwebtoken": "^9.0.3",
		"serverless-http": "^4.0.0"
	}
}
